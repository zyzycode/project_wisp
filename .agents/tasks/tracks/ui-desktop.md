# Трек: UI & Desktop Interaction

Исторический трек контекстного меню, Debug HUD, инспектора анимаций и визуальных интерфейсов. Новые задачи создаются в [GitHub Issues](https://github.com/zyzycode/project_wisp/issues) и добавляются в [Project Wisp — Delivery](https://github.com/users/zyzycode/projects/1).

---

## 1. Текущий статус

- [x] **P08:** Базовый интерфейс чата и речевого облака. (`done`)
- [x] **P13-HUD:** Структурированный логгер и базовая панель Debug HUD. (`done`)
- [x] **P14-UI01:** Расширение контекстного меню (быстрые взаимодействия, ручная смена поз, тумблер Debug HUD). (`done` / `app-developer`)
- [x] **P14-UI01-REV:** Ревью-гейт контекстного меню и позиционирования (`Approved`). (`done` / `reviewer`)
- [x] **P14-UI02:** Инспектор анимаций и анкоров в Debug HUD (ручной запуск клипов и проверка оверлея). (`done` / `app-developer`)
- [x] **P14-UI02-REV:** Ревью-гейт инспектора анимаций и визуализатора анкоров (`Approved`). (`done` / `reviewer`)
- [x] **P14-UI03:** Улучшение речевого облачка (авто-скрытие, тайминги, анимация появления). (`done` / `app-developer`)
- [x] **P14-UI03-REV:** Ревью-гейт речевого облачка и динамических таймингов (`Approved`). (`done` / `reviewer`)

---

## 2. Подробные карточки задач

Все активные задачи трека UI & Desktop Interaction в рамках текущего спринта реализованы и верифицированы. Следующие расширения создаются в GitHub по мере readiness Phase 15.
