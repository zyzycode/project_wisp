# Трек: UI & Desktop Interaction

Файл бэклога контекстного меню, панели отладки (Debug HUD), инспектора анимаций и визуальных интерфейсов.

---

## 1. Текущий статус

- [x] **P08:** Базовый интерфейс чата и речевого облака. (`done`)
- [x] **P13-HUD:** Структурированный логгер и базовая панель Debug HUD. (`done`)
- [x] **P14-UI01:** Расширение контекстного меню (быстрые взаимодействия, ручная смена поз, тумблер Debug HUD). (`done` / `app-developer`)
- [x] **P14-UI01-REV:** Ревью-гейт контекстного меню и позиционирования (`Approved`). (`done` / `reviewer`)
- [x] **P14-UI02:** Инспектор анимаций и анкоров в Debug HUD (ручной запуск клипов и проверка оверлея). (`done` / `app-developer`)
- [x] **P14-UI02-REV:** Ревью-гейт инспектора анимаций и визуализатора анкоров (`Approved`). (`done` / `reviewer`)
- [ ] **P14-UI03:** Улучшение речевого облачка (авто-скрытие, тайминги, анимация появления). (`ready` / `app-developer`)

---

## 2. Подробные карточки задач

### [TASK: P14-UI03] — Speech Bubble Auto-Dismiss & Polish
- **Исполнитель:** `app-developer`
- **Зависит от:** `P14-UI01`
- **Цель:** Настройка плавной анимации появления/скрытия речевого облачка, тайминги авто-скрытия в зависимости от длины текста и адаптивное позиционирование относительно персонажа.
- **Читать:**
  - `.agents/agents/app-developer/agent.md`
  - `src/renderer/components/Chat/SpeechBubble.tsx`
- **Менять:** `src/renderer/components/Chat/SpeechBubble.tsx`, тесты.
- **Критерии приёмки:**
  - [ ] Облачко автоматически закрывается по таймеру чтения (минимум 3 секунды + добавочное время от длины сообщения).
  - [ ] Облачко не перекрывает персонажа и адаптируется при приближении к границе экрана.
  - [ ] Все тесты проходят.
