# SPRITE-LOOK-PACK-V2 — 2026-09-18

По запросу пользователя полностью перегенерирован четырёхкадровый пак
`body_look_around`. Встроенный `image_gen` использовал старый лист как референс
поз и персонажа, а `body_sit_edge_00.png` — как референс лица.
[Точный промпт](look-around-faces-v2.prompt.txt).

Сначала сохранён исходник
[generated_images/body_look_around_faces_v2.png](../generated_images/body_look_around_faces_v2.png),
затем выполнена нарезка. Старый исходник сохранён. Новый лист RGBA 2172×724,
четыре полных силуэта слева направо, прозрачность 0–255. Лица уже нарисованы
в исходнике: при повороте дальний глаз сужается и закрывается чёлкой.
Наложение отдельных `face_*` удалено из рецепта этого набора.

Существующий `extract_reviewed` выделяет четыре силуэта размерами 477×676,
463×673, 482×671 и 484×676. Одинаковый масштаб 148/256 по высоте головы,
центр X=256 и нижняя граница Y=460 сохраняют согласованный размер персонажа.
Результат — четыре RGBA PNG 512×512 в
[public/assets/sprites/body/look_around](../../public/assets/sprites/body/look_around/).
В манифесте обновлён только `body_look_around.sourceFile`; режим `baked_in`,
порядок, URL, длительности и остальные 47 записей сохранены.

Пак и разрешённые предыдущие PNG зафиксированы SHA-256 в
[рецепте](normalization-2026-09.json). Повторный экспорт воспроизводит нарезку;
обычный обработчик даже с `--force` пропускает этот проверенный исходник.

## Проверка

Просмотрены весь исходник и все четыре конечных кадра на светлом и тёмном
фоне. Проверены перспектива глаз, перекрытие чёлкой, целые силуэты, масштаб
и положение стоп. Превью в `../output/normalization/`:
`body_look_around-light.jpg`, `body_look_around-dark.jpg`, `body_look_around.gif`.

- Регрессионный тест до изменения воспроизвёл отсутствие исходного пака
  и наличие приклеивания face_*; после изменения проходит.
- 40 Python-тестов пройдены; `normalize_reviewed.py --check` успешен.
- Валидатор: 200 PNG, 48 анимаций, 0 ошибок и предупреждений.
- Загрузчик манифеста/резолвер: 69 тестов пройдены.
- `npm run typecheck`: успешно.
- `npm run test`: 716 passed, 2 skipped. Ссылки и `git diff --check` проверены.

Scope: только этот пак, его четыре PNG, рецепт, тест и документация пайплайна,
`sourceFile` в манифесте. Renderer не менялся; сборка не запускалась.
Нативная проверка Windows не выполнялась. Следующий gate: `reviewer`.
